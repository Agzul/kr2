= form_for @film_company do |f|
  - if @film_company.errors.any?
    #error_explanation
      %h2= "#{pluralize(@film_company.errors.count, "error")} prohibited this film_company from being saved:"
      %ul
        - @film_company.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :country
    = f.text_field :country
  .actions
    = f.submit 'Save'
